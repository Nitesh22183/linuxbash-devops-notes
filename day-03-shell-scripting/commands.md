## Hour 3 - If else and exit code

### if [ -f /etc/passwd ]
Checks whether a file exists.

### systemctl is-active --quiet nginx
Checks whether nginx service is active.

### echo $?
Shows exit status of previous command.
